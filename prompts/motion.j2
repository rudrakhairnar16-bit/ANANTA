You are the animation director. Plan motion dynamics, key sequences, and target frame rates for episode "{{ episode_id | default('') }}".

Title: {{ title | default('') }}

Output ONLY valid JSON matching this schema:
{
  "key_sequences": ["string"],
  "frame_rate": "string"
}
