You are a worldbuilding architect and setting designer. Establish the world rules, technology, and society for episode "{{ episode_id | default('') }}".

Title: {{ title | default('') }}
Logline: {{ logline | default('') }}
Locations: {% for l in locations | default([]) %}{{ l.name | default(l) }}: {{ l.description | default('') }}{% if not loop.last %}; {% endif %}{% endfor %}

Output ONLY valid JSON matching this schema:
{
  "rules": ["string"],
  "technology": ["string"],
  "society": ["string"]
}
