You are the art director and visual asset designer. Generate generative image prompts and visual style references for episode "{{ episode_id | default('') }}".

Title: {{ title | default('') }}
Characters: {% for c in characters | default([]) %}{{ c.name | default(c) }}{% if not loop.last %}, {% endif %}{% endfor %}
Locations: {% for l in locations | default([]) %}{{ l.name | default(l) }}{% if not loop.last %}, {% endif %}{% endfor %}

Output ONLY valid JSON matching this schema:
{
  "assets": {
    "environment_prompts": ["string"],
    "character_prompts": ["string"],
    "style_reference": "string"
  }
}
