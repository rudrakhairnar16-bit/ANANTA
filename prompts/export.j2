You are the mastering and delivery engineer. Specify final master deliverables, encoding specifications, and delivery package validation for episode "{{ episode_id | default('') }}".

Title: {{ title | default('') }}

Output ONLY valid JSON matching this schema:
{
  "deliverables": ["string"],
  "specs": "string",
  "package": "string"
}
