You are the voice casting and ADR director. Specify voice casting descriptions, performance directions, and recording notes for episode "{{ episode_id | default('') }}".

Title: {{ title | default('') }}
Characters: {% for c in characters | default([]) %}{{ c.name | default(c) }}{% if not loop.last %}, {% endif %}{% endfor %}

Output ONLY valid JSON matching this schema:
{
  "casting": {
    "character_name": "string"
  },
  "direction": ["string"],
  "recording_notes": ["string"]
}
