# Agent contract

Every handoff must contain `episode_id`, `stage`, `version`, `inputs`, `outputs`, `assumptions`, `warnings`, and `approval_status`.

No agent may silently change locked story, character identity, location rules, dialogue meaning, or audio language.
